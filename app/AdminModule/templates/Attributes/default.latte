{block buttons}
        <a class="btn btn-primary" n:href=":Admin:Attributes:add" n:if="$user->identity->role==9"><i class="fas fa-plus"></i> Přidat atribut</a>
{/block}

{block content}
      <div class="head">
        <h1 class="card-title">
          Správa atributů
        </h1>
      </div>

	  <div class="sortable table-responsive" data-action="{link sort!}">
	  	{control attributes}
	  </div>

{/block}
