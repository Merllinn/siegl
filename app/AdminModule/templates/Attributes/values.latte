{block content}


      <div class="head">
        <h1 class="title">
          Správa hodnot atributu {$attribute->name}
        </h1>
        <a class="btn btn-primary" n:href=":Admin:Attributes:addValue $attribute->id" n:if="$user->identity->role==9"><i class="fas fa-plus"></i> Přidat hodnotu</a>
      </div>

	  <div class="sortable table-responsive" data-action="{link sort!}">
	  	{control values}
	  </div>

{/block}
