{block buttons}
	        <a class="btn btn-primary" n:href="add" n:if="$user->identity->role==9"><i class="fas fa-plus"></i> Přidat osobu</a>
{/block}

{block content}

	      <div class="head">
	        <h1 class="card-title">Osoby</h1>
	      </div>

	      <div class="sortable table-responsive" data-action="{link sort!}">
	      	{control users}
	      </div>
{/block}
